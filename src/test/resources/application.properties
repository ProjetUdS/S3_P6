## Utilise une version locale de la db "app", pour que les
##  tests aient un environnement reproductible
%test.quarkus.datasource.db-kind=postgresql
%test.quarkus.datasource.username=test
%test.quarkus.datasource.password=test

##Utilise un port libre pour éviter les conflits
%test.quarkus.http.port=0

# Provide dummy/default values for other required configurations to bypass verification
%test.quarkus.oidc.auth-server-url=http://localhost:8080/realms/usager
%test.quarkus.oidc.client-id=test-client
%test.quarkus.oidc.credentials.secret=test-secret
%test.minio.url=http://localhost:9000
%test.minio.access.key=minioadmin
%test.minio.secret.key=minioadmin