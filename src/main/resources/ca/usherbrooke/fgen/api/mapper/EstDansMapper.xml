<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ca.usherbrooke.fgen.api.mapper.EstDansMapper">

    <select id="select" resultType="ca.usherbrooke.fgen.api.business.EstDans">
        SELECT cip, equipe_id
        FROM app.est_dans
        WHERE (cip, equipe_id) =
              (coalesce(#{cip}, cip),
               coalesce(#{equipeId}, equipe_id))
    </select>

    <insert id="insertEstDans">
        INSERT INTO app.est_dans(cip, equipe_id)
        VALUES (#{estDans.cip}, #{estDans.equipeId})
            ON CONFLICT (cip, equipe_id) DO NOTHING
    </insert>

    <delete id="deleteOne">
        DELETE
        FROM app.est_dans
        WHERE cip = #{cip}
          AND equipe_id = #{equipeId}
    </delete>

</mapper>