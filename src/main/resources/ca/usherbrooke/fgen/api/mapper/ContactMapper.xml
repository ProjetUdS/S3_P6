<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ca.usherbrooke.fgen.api.mapper.ContactMapper">

    <insert id="insertContact">
        INSERT INTO app.Contact(cip, cip_contact)
        VALUES (#{cip}, #{cipContact})
        ON CONFLICT DO NOTHING
    </insert>

    <delete id="deleteContact">
        DELETE FROM app.Contact
        WHERE cip = #{cip}
          AND cip_contact = #{cipContact}
    </delete>

    <select id="selectContacts" resultType="String">
        SELECT cip_contact
        FROM app.Contact
        WHERE cip = #{cip}
    </select>

    <select id="isContact" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM app.Contact
            WHERE cip = #{cip}
              AND cip_contact = #{cipContact}
        )
    </select>

</mapper>