Production build
=
#### You can only run the app if you are in the list of authorized user (sops)
To run the app:
1. build it using
```sh
./build.sh
``` 

2. run it using: 
```sh
./start_script.sh
```